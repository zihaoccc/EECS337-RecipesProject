1. URL parser - entry point that calls the ingredient parser and the directions parser
2. Ingredient parser -> {name, description, quantity, measurement, preparation}
3. A bit of the command line interface for transformations.